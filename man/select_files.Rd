% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/data_management.R
\name{select_files}
\alias{select_files}
\title{Unified file selection function}
\usage{
select_files(
  pattern = NULL,
  exclude = NULL,
  recent = NULL,
  set_name = NULL,
  folder = "source",
  full_path = TRUE,
  interactive = FALSE,
  file_extension = NULL
)
}
\arguments{
\item{pattern}{Keywords or regex pattern to match filenames}

\item{exclude}{Keywords to exclude from results (can be vector)}

\item{recent}{Time specification: "30m", "2h", "1d" etc}

\item{set_name}{Name of existing file set to retrieve}

\item{folder}{Folder to search: "source", "cleaned", etc (default: "source")}

\item{full_path}{Return full paths (default: TRUE)}

\item{interactive}{Use interactive selection (default: FALSE)}

\item{file_extension}{Filter by extension (default: NULL for all files)}
}
\value{
Vector of file paths (full or relative based on full_path parameter)
}
\description{
One function to handle all file selection methods: pattern matching,
recent files, file sets, and interactive selection.
}
\examples{
# Select by pattern
select_files(pattern = "baseline", folder = "cleaned")

# Select recent files
select_files(recent = "2h", folder = "source")

# Select with exclusions
select_files(pattern = "data", exclude = c("old", "temp"))

# Get a file set
select_files(set_name = "baseline_series")
}
